engine-strict=true
# @visorcraft/mongreldb-kit 0.47.0 still declares peer @visorcraft/mongreldb ^0.46.2
# even though it is built for MongrelDB 0.47.0; relax strict peer resolution so
# `npm ci` works until the upstream peer range is widened.
legacy-peer-deps=true
